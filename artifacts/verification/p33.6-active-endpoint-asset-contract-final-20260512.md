# P33.6 — Active Endpoint Asset Contract Final

## Tarih
2026-05-12

## Karar

```
P33.6 ACTIVE ENDPOINT ASSET CONTRACT — PASS
89/89 test geçti.
assets.cover backend zincirinde korunuyor.
Active endpoint response içinde assets.cover dönecek.
P33.7 Android Asset Loader'a geçilebilir.
```

---

## 1. Discovery Bulguları

`cover` alanının backend zincirinde kaybolduğu iki kopma noktası tespit edildi:

| # | Katman | Dosya | Sorun |
|---|--------|-------|-------|
| 1 | Type contract | `contracts.ts:242-247` | `AssetManifestEntity` içinde `cover` tanımlı değil |
| 2 | Validation DTO | `games.module.ts:302-318` | `AssetsDto` içinde `cover` alanı yok → `whitelist: true` + `forbidNonWhitelisted: true` nedeniyle 400 dönüyor |

**Root cause**: `main.ts:15-19` — `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true })` konfigürasyonu, DTO'da tanımlı olmayan `cover` alanını istekten atıyor ve 400 hatası döndürüyor.

## 2. Uygulanan Değişiklikler

### 2.1 `contracts.ts` — AssetManifestEntity

```diff
 export interface AssetManifestEntity {
+  cover?: string;
   background: string;
   character: string;
   soundtrack?: string;
   items?: GameAssetEntity[];
 }
```

### 2.2 `games.module.ts` — AssetsDto

```diff
 class AssetsDto {
+  @IsOptional()
+  @IsString()
+  cover?: string;
+
   @IsString()
   background!: string;
   ...
 }
```

Bu iki değişiklikle `cover` alanı tüm zincirde korunur hale geldi.

## 3. Zincir Doğrulaması

| Adım | Katman | cover durumu |
|------|--------|-------------|
| 1 | Admin `PATCH /internal/game-definitions/:id` | `assets.cover` request body'de |
| 2 | `UpdateGameDefinitionDto` → `AssetsDto` | `@IsOptional() cover?: string` → KABUL |
| 3 | `GamesService.update()` | `assets: dto.assets ?? game.assets` → korunur |
| 4 | `GameDefinitionsRepository.save()` | `assets: entity.assets as Prisma.InputJsonValue` → JSON saklanır |
| 5 | `mapPersistedGameDefinition()` | `assets: definition.assets as ...` → JSON'dan okunur |
| 6 | `mapGameDefinitionToResponse()` | `assets: game.assets` → aynen iletilir |
| 7 | `GET /v1/game-definitions/active` | response içinde `assets.cover` görünür |
| 8 | `content-publish` line 180 | `assets: game.assets` → aynen iletilir |

## 4. Test Sonuçları

```
Test Suites: 5 passed, 5 total
Tests:       89 passed, 89 total
```

| Test suite | Test sayısı | Sonuç |
|---|---|---|
| game-definition-v3.spec.ts | geçti | PASS |
| publish-validation.spec.ts | geçti | PASS |
| game-validation-phase2.spec.ts | geçti | PASS |
| game-sessions.spec.ts | geçti | PASS |
| profiles.spec.ts | geçti | PASS |

## 5. Geriye Dönük Uyumluluk

| Durum | Beklenen | Gerçek |
|-------|----------|--------|
| Eski oyun (cover yok) | Bozulmaz | `cover` optional — eski oyunlar aynen çalışır |
| Yeni oyun (cover var) | cover korunur | DTO kabul eder → entity saklar → response döner |
| background ≠ cover | Ayrı kalır | `cover` ve `background` bağımsız alanlar, fallback yok |
| Seeded games | Etkilenmez | `seededGames` dizisinde cover alanı yok, optional olduğu için sorun yok |
| Publish validation | Engellenmez | `hasRequiredAssets()` cover'a bakmaz |

## 6. Güvenlik

| Kontrol | Sonuç |
|---|---|
| Token/secret değişikliği | YOK |
| PHP_ASSET_TOKEN frontend'de | YOK |
| Private path response'ta | YOK (sadece public asset URL'leri) |
| Active endpoint internalOnly filtreleme | `isInternalGame()` korundu |

## 7. Değiştirilen Dosyalar

```
backend/src/common/contracts.ts          — AssetManifestEntity.cover eklendi
backend/src/games/games.module.ts       — AssetsDto.cover eklendi
```

Toplam: 2 dosya, 5 satır ekleme.

---

## Sonraki Adım

```
P33.7 Android Asset Loader
```
