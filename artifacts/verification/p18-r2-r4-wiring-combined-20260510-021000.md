# P18.2-P18.5 Wiring Combined

**Date:** 2026-05-10 02:10:00
**Decision: PASS**

---

## Changes

### AlbaMotionController.kt

- Added `GameSessionRepository` field (lazy, from `AlbaDatabase.getInstance(appContext)`)
- Added `gameSessionRepo` public getter
- Modified `syncFinishedGame()`:
  - `saveAndEnqueue()` before sync attempt (Room local save + queue)
  - `GameSessionSyncWorker.enqueue()` to start WorkManager
  - Fast-path direct sync kept for immediate feedback
  - On success: `markSynced()` in Room
  - On failure: queue handles retry, user sees "Cihaza kaydedildi"

### ProfileScreen.kt

- Added `repository: GameSessionRepository?` parameter
- Real Room Flow data: totalGames, totalDuration, topScore, syncedCount, pendingCount
- Recent sessions list with game title, score, sync status icon
- Empty state: "Ilk oyununu oynadiginda burada gorunecek"
- Honest messaging preserved

### AlbaApplication.kt

- Updated `PROFILE` destination to pass `controller.gameSessionRepo`

## Data Flow

```
Game Finish → saveAndEnqueue(Room) → Worker.enqueue()
           → Fast sync (OkHttp)
           → Success: markSynced(Room)
           → Failure: worker retries later
Profile ← Room Flows (totalGames, recentSessions, syncStatus)
```
